# proxy-manager (Go) — see CONTRACTS.md section 2.
# All of these are read at startup. Cooldown/lease values are in seconds.

PORT=8080
PROXY_POOL_FILE=/app/proxies.json
FAILURE_THRESHOLD=3
COOLDOWN_BASE_SECONDS=30
COOLDOWN_MAX_SECONDS=600
LEASE_TTL_SECONDS=120
LOG_LEVEL=info